#!/bin/bash 
args=$@
# test first
curDir=`pwd`
testDir=$HOME
buildLog="/tmp/"$(USER)-log-x879873
./runSelfTest &> $buildLog 
ret=$?

if ["$ret" == "0"]; then 
	exit 0
fi 
# if  test pass, then commit
echo svn ci $@
# test after ci
cd $testDir 
svn co  
cd matrixseer_test/test/
